# Vercel Marketplace 中安装 Upstash Redis 后会自动注入以下变量。
# 本地不设置时应用会回退到进程内存，仅适合单机开发。
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# 可选：同一个 Redis 中部署多套评分应用时，用不同的 key 隔离数据。
COMPETITION_STATE_KEY="vote:competition:state:v2"
